# Pinned for reproducible installs. Re-pin with `pip-compile` when bumping.

# Web framework
Flask==3.1.3
Werkzeug==3.1.6

# Auth / session
Flask-Login==0.6.3

# CSRF protection (every POST form is tokenised; see app/__init__.py)
Flask-WTF==1.2.1

# Brute-force protection on /login
Flask-Limiter==3.8.0

# Persistence
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.35

# Computer vision / recognition
opencv-python==4.10.0.84
face_recognition==1.3.0
numpy==1.26.4

# Production WSGI server (used by Dockerfile)
gunicorn==23.0.0
